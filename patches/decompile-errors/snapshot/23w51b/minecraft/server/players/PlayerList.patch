diff --git a/minecraft/server/players/PlayerList.java b/minecraft/server/players/PlayerList.java
index fc1a9e2..542934c 100644
--- a/minecraft/server/players/PlayerList.java
+++ b/minecraft/server/players/PlayerList.java
@@ -147,20 +147,20 @@ public abstract class PlayerList {
     public void placeNewPlayer(Connection connection, ServerPlayer serverplayer, CommonListenerCookie commonlistenercookie) {
         GameProfile gameprofile = serverplayer.getGameProfile();
         GameProfileCache gameprofilecache = this.server.getProfileCache();
-        Optional optional;
+        Optional<CompoundTag> optional;
         String s;
 
         if (gameprofilecache != null) {
-            optional = gameprofilecache.get(gameprofile.getId());
-            s = (String) optional.map(GameProfile::getName).orElse(gameprofile.getName());
+            Optional<GameProfile> optional1 = gameprofilecache.get(gameprofile.getId());
+            s = (String) optional1.map(GameProfile::getName).orElse(gameprofile.getName());
             gameprofilecache.add(gameprofile);
         } else {
             s = gameprofile.getName();
         }
 
         optional = this.load(serverplayer);
-        ResourceKey<Level> resourcekey = (ResourceKey) optional.flatMap((compoundtag) -> {
-            DataResult dataresult = DimensionType.parseLegacy(new Dynamic(NbtOps.INSTANCE, compoundtag.get("Dimension")));
+        ResourceKey<Level> resourcekey = optional.flatMap((compoundtag) -> {
+            DataResult<ResourceKey<Level>> dataresult = DimensionType.parseLegacy(new Dynamic(NbtOps.INSTANCE, compoundtag.get("Dimension")));
             Logger logger = PlayerList.LOGGER;
 
             Objects.requireNonNull(logger);
@@ -182,7 +182,7 @@ public abstract class PlayerList {
         PlayerList.LOGGER.info("{}[{}] logged in with entity id {} at ({}, {}, {})", new Object[]{serverplayer.getName().getString(), s1, serverplayer.getId(), serverplayer.getX(), serverplayer.getY(), serverplayer.getZ()});
         LevelData leveldata = serverlevel1.getLevelData();
 
-        serverplayer.loadGameTypes((CompoundTag) optional.orElse((Object) null));
+        serverplayer.loadGameTypes((CompoundTag) optional.orElse(null));
         ServerGamePacketListenerImpl servergamepacketlistenerimpl = new ServerGamePacketListenerImpl(this.server, connection, serverplayer, commonlistenercookie);
         GameRules gamerules = serverlevel1.getGameRules();
         boolean flag = gamerules.getBoolean(GameRules.RULE_DO_IMMEDIATE_RESPAWN);
@@ -658,7 +658,7 @@ public abstract class PlayerList {
     }
 
     public void deop(GameProfile gameprofile) {
-        this.ops.remove((Object) gameprofile);
+        this.ops.remove(gameprofile);
         ServerPlayer serverplayer = this.getPlayer(gameprofile.getId());
 
         if (serverplayer != null) {
